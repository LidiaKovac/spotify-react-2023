import { Action, ActionCreator, AnyAction, createAsyncThunk, Dispatch, Selector, ThunkAction, ThunkDispatch } from "@reduxjs/toolkit"
import { RootState } from "../app/store"

export const GET_SONG_BY_QUERY = "GET_SONG_BY_QUERY"

